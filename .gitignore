src/rust/vendor
src/Makevars
src/Makevars.win

.RData
.Rhistory
.DS_Store

.claude/worktrees/

# pkgdown site is built in CI and served from the gh-pages branch
docs/

# scratch / working directory — never committed
tmp/

# large (~20 MB) upstream source dataset; the derived, shareable
# EnglandWalesMeasles_places.csv (built by convert_measles.py) is committed instead
examples/data/EnglandWalesMeasles.py

# Local vendored clones (not part of the package)
worldwide/

# Local RStudio knit/preview artifacts of the teaching articles. The canonical
# rendered view is the pkgdown site (built into docs/, above, and deployed to gh-pages).
vignettes/articles/*.html
vignettes/articles/*.nb.html
