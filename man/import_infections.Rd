% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{import_infections}
\alias{import_infections}
\title{Import new infectious cases from a schedule, activating reserved agent slots.}
\usage{
import_infections(
  state,
  timer,
  nodeid,
  count,
  i_count,
  importations,
  sched_tick,
  sched_node,
  sched_count,
  duration,
  tick
)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (capacity-sized; imported slots set to I).}

\item{timer}{Per-agent \code{u16} timer Column (imported slots set from \code{duration}).}

\item{nodeid}{Per-agent \code{u16} node-id Column (imported slots set to their node).}

\item{count}{Current active agent count (the first free slot).}

\item{i_count}{\verb{n_ticks x n_nodes} i32 I census Column (mutated at \code{tick + 1}).}

\item{importations}{\verb{(n_ticks-1) x n_nodes} i32 flow Column (set at \code{tick}).}

\item{sched_tick, sched_node, sched_count}{Equal-length integer schedule vectors.}

\item{duration}{A Distribution for the imported cases' infectious timer.}

\item{tick}{0-based tick index.}
}
\value{
The new active agent count (\code{count} plus the number imported this tick).
}
\description{
For the given \code{tick}, scans the schedule (parallel \code{sched_tick} / \code{sched_node} /
\code{sched_count} vectors) and, for every entry whose \code{sched_tick == tick}, activates
\code{sched_count} new agents in node \code{sched_node}: each takes the next free slot after
the active \code{count}, is set Infectious with a \code{timer} drawn from \code{duration}, and
has its \code{nodeid} set. The new agents must fit in the reserved capacity (the
\code{state}/\code{timer}/\code{nodeid} Columns are allocated larger than the initial \code{count}).
Per-node import counts are added to the I census at column \code{tick + 1} and written
to the \code{importations} flow at column \code{tick}.
}
\details{
Returns the new active agent count (the caller stores it back into \code{people$count}).
Sequential — it touches only the handful of imported slots, not all agents.
}
