% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_capacity.R
\name{calc_capacity_cdr}
\alias{calc_capacity_cdr}
\title{Estimate the agent capacity for a growing population reclaimed with \code{\link[=squash]{squash()}}.}
\usage{
calc_capacity_cdr(birthrates, deathrates, initial_pop, safety_factor = 1)
}
\arguments{
\item{birthrates}{A 2-D \verb{nsteps x nnodes} matrix of crude birth rates (per 1,000 per
year), or a 2-D \link{Column} (e.g. from \code{\link[=values_map]{values_map()}}). Each value in \verb{[0, 100]}.}

\item{deathrates}{A 2-D \verb{nsteps x nnodes} matrix (or 2-D \link{Column}) of crude death rates,
the same shape as \code{birthrates}. Each value in \verb{[0, 100]}.}

\item{initial_pop}{A non-negative numeric vector of length \code{nnodes}: initial population.}

\item{safety_factor}{Non-negative headroom multiplier in \verb{[0, 6]} (default \code{1}),
controlling how much the death rate is underestimated. \code{0} credits deaths fully.}
}
\value{
A numeric vector of length \code{nnodes} of estimated capacities (whole-valued
doubles), each at least the node's \code{initial_pop} (a net-shrinking projection cannot
report below the starting population, whose peak living count occurs at \code{t = 0}). A
\code{warning} is issued if any estimate exceeds \code{.Machine$integer.max}.
}
\description{
The mortality-aware companion to \code{\link[=calc_capacity]{calc_capacity()}}. When dead agents' slots are
reclaimed periodically with \code{\link[=squash]{squash()}}, the slots needed are bounded by the \strong{peak
simultaneous living population} — net births minus deaths — not by the cumulative
number ever born (which \code{\link[=calc_capacity]{calc_capacity()}} estimates for the no-reclaim case). This lets
you model decades or centuries without allocating one slot per agent ever born.
}
\details{
The per-node daily birth and death rates \code{lambda = (1 + rate/1000)^(1/365) - 1} are
summed over all time steps; the expected net-growth factor is \verb{exp(sum lambda_b - sum lambda_d)}. For a conservative bound the \strong{death rate is underestimated} by the safety
factor — only a fraction \code{1 / (1 + safety_factor)} of the death sum is credited, holding
the rest back as headroom against a lower-mortality (faster-growing) realization. So
\code{safety_factor = 0} credits deaths fully (the tightest, bare net-growth estimate); larger
values credit fewer deaths and reserve more slack. (Unlike \code{\link[=calc_capacity]{calc_capacity()}}, no gross-
births term enters — that would defeat the point of reclaiming slots.)
}
\section{Errors}{

Stops if either rate grid is not 2-D or holds NA / non-finite values, if their shapes or
node counts disagree with each
other or with \code{length(initial_pop)}, if any population is negative, if any rate is
outside \verb{[0, 100]}, or if \code{safety_factor} is outside \verb{[0, 6]}.
}

\examples{
# one node, 100 years, CBR 30 / CDR 15 — peak-living bound for a squash-reclaimed run
br <- matrix(30, nrow = 100 * 365, ncol = 1)
dr <- matrix(15, nrow = 100 * 365, ncol = 1)
calc_capacity_cdr(br, dr, initial_pop = 1e6, safety_factor = 1)
}
\seealso{
\code{\link[=calc_capacity]{calc_capacity()}} (cumulative-births bound, no reclaim), \code{\link[=squash]{squash()}}.
}
