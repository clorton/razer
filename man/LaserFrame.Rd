% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/laser_frame.R
\docType{data}
\name{LaserFrame}
\alias{LaserFrame}
\alias{$.LaserFrame}
\alias{[[.LaserFrame}
\alias{$<-.LaserFrame}
\title{Fixed-capacity struct-of-arrays population or patch data store.}
\format{
An object of class \code{environment} of length 17.
}
\usage{
LaserFrame
}
\description{
Mirrors \code{laser.core.LaserFrame} from Python. Each property occupies a
pre-allocated backing array; \code{count} tracks how many entries are active.
}
\details{
\strong{Scalar properties} store one value per entry (length = \code{capacity}).

\strong{Vector properties} store \code{length} values per entry, laid out column-major
so that \code{get_col(name, col)} returns a contiguous memory slice. This is
R's native matrix storage order and makes per-tick node-state updates fast.
}
\section{Methods}{

\subsection{Method \code{new}}{
Create a new \code{LaserFrame}.

\subsection{Arguments}{
\describe{
\item{\code{capacity}}{Maximum number of entries (agents or nodes). Must be positive.}
\item{\code{initial_count}}{Number of entries active at construction. Pass \code{-1} (the default) to set active count equal to \code{capacity}.}
}}
\subsection{return}{
A new \code{LaserFrame} object.
}
\subsection{export}{

}
}

\subsection{Method \code{count}}{
Number of currently active entries.
\subsection{export}{

}
}

\subsection{Method \code{capacity}}{
Total capacity (fixed at construction).
\subsection{export}{

}
}

\subsection{Method \code{scalar_names}}{
Names of all scalar properties, sorted alphabetically.
\subsection{export}{

}
}

\subsection{Method \code{vector_names}}{
Names of all vector properties, sorted alphabetically.
\subsection{export}{

}
}

\subsection{Method \code{vector_ncols}}{
Number of columns in a named vector property.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Vector property name.}
}}
\subsection{export}{

}
}

\subsection{Method \code{describe}}{
Human-readable summary of the frame's properties and memory layout.
\subsection{export}{

}
}

\subsection{Method \code{add_scalar_property}}{
Add a scalar property (one value per entry).

\subsection{Arguments}{
\describe{
\item{\code{name}}{Property name. Must not already exist.}
\item{\code{dtype}}{\code{"integer"}, \code{"real"}, or \code{"logical"}.}
\item{\code{default}}{Fill value for the backing array.}
}}
\subsection{export}{

}
}

\subsection{Method \code{add_vector_property}}{
Add a vector property (\verb{capacity × length}, column-major).

The backing array has \code{capacity * length} elements.
Element \verb{[entry, col]} is stored at offset \code{entry + col * capacity}.
Column \code{col} (all active entries for one time-step) is contiguous.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Property name. Must not already exist.}
\item{\code{length}}{Number of columns (e.g., \code{nticks + 1} for a time-series).}
\item{\code{dtype}}{\code{"integer"}, \code{"real"}, or \code{"logical"}.}
\item{\code{default}}{Fill value.}
}}
\subsection{export}{

}
}

\subsection{Method \code{add}}{
Activate \code{n} additional entries and return their 1-based index range.

Returns \code{c(start, end)} (both inclusive, 1-based) so that
\code{frame$get("prop")[start:end]} addresses the newly activated entries.

\subsection{Arguments}{
\describe{
\item{\code{n}}{Number of entries to activate.}
}}
\subsection{export}{

}
}

\subsection{Method \code{squash}}{
Compact active entries, keeping only those where \code{mask} is \code{TRUE}.

All scalar and vector properties are squashed in place.
\code{count} is updated to the number of kept entries.

\subsection{Arguments}{
\describe{
\item{\code{mask}}{Logical vector of length \code{count}.}
}}
\subsection{export}{

}
}

\subsection{Method \code{sort_by}}{
Reorder active scalar properties by \code{perm} (1-based permutation of length \code{count}).

Each property is permuted in parallel across available CPU threads via Rayon.
Only scalar properties are reordered; vector properties (time-series) are left unchanged.

\subsection{Arguments}{
\describe{
\item{\code{perm}}{Integer vector of length \code{count}. Must be a valid permutation of \code{1:count}.}
}}
\subsection{export}{

}
}

\subsection{Method \code{get}}{
Return the active slice of a scalar property as an R vector.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Scalar property name.}
}}
\subsection{export}{

}
}

\subsection{Method \code{set}}{
Overwrite the active slice of a scalar property from an R vector.

Integer and real vectors are accepted for integer and real properties
respectively; integer values are accepted for real properties (coerced).

\subsection{Arguments}{
\describe{
\item{\code{name}}{Scalar property name.}
\item{\code{values}}{R vector of length \code{count}.}
}}
\subsection{export}{

}
}

\subsection{Method \code{get_col}}{
Return one column of a vector property as an R vector.

The column is returned for active entries only (length = \code{count}).
Columns are \strong{1-based}: column 1 is the first column.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Vector property name.}
\item{\code{col}}{Column index (1-based).}
}}
\subsection{export}{

}
}

\subsection{Method \code{set_col}}{
Overwrite one column of a vector property from an R vector.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Vector property name.}
\item{\code{col}}{Column index (1-based).}
\item{\code{values}}{R vector of length \code{count}.}
}}
\subsection{export}{

}
}

\subsection{Method \code{get_matrix}}{
Return a vector property as an R matrix of shape \verb{(count, ncols)}.

Because both R and the backing store use column-major layout, each
column of the returned matrix is a direct contiguous copy.

\subsection{Arguments}{
\describe{
\item{\code{name}}{Vector property name.}
}}
\subsection{export}{

}
}
}

\keyword{datasets}
