% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_capacity.R
\name{calc_capacity}
\alias{calc_capacity}
\title{Estimate the agent capacity needed for a growing population.}
\usage{
calc_capacity(birthrates, initial_pop, safety_factor = 1)
}
\arguments{
\item{birthrates}{A 2-D \verb{nsteps x nnodes} numeric matrix of crude birth rates (births
per 1,000 individuals per year), or a 2-D razer \link{Column} (e.g. from \code{\link[=values_map]{values_map()}})
whose \verb{$values()} is such a matrix. Each value must be in \verb{[0, 100]}.}

\item{initial_pop}{A numeric vector of length \code{nnodes}: the initial population per
node. Must be non-negative.}

\item{safety_factor}{Non-negative headroom multiplier in \verb{[0, 6]} (default \code{1}). \code{0}
gives the bare expected-growth estimate; larger values reserve more slack.}
}
\value{
A numeric vector of length \code{nnodes} of estimated capacities (whole-valued
doubles, which represent integers exactly up to \code{2^53}). A \code{warning} is issued if
any estimate exceeds \code{.Machine$integer.max} (R's 32-bit signed integer max) — the
largest count razer's allocators (\code{allocate_scalar}/\code{allocate_vector}, whose
\code{count} is an \code{i32}) can accept.
}
\description{
Projects each node's population forward under its (possibly time-varying) crude birth
rate and returns a per-node capacity to preallocate, inflated by an optional safety
factor. The births are treated as geometric growth: a daily growth rate
\code{lambda = (1 + CBR/1000)^(1/365) - 1} is summed over all time steps and exponentiated
(\verb{exp(sum lambda)}), a geometric-Brownian-motion-style estimate of expected growth.
The safety factor adds a multiple of the growth's standard-deviation-like term
\verb{sqrt(exp(sum lambda)) - 1} as headroom for stochastic variation.
}
\section{Errors}{

Stops if \code{birthrates} is not 2-D, if its node count does not match \code{length(initial_pop)},
if any population is negative, if any birth rate is outside \verb{[0, 100]}, or if
\code{safety_factor} is outside \verb{[0, 6]}.
}

\examples{
# two nodes, one year of a constant CBR of 40 per 1,000
br <- matrix(40, nrow = 365, ncol = 2)
calc_capacity(br, initial_pop = c(1e6, 5e5), safety_factor = 1)
}
