% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{dist_gamma}
\alias{dist_gamma}
\title{Create a gamma distribution parameterized by shape and scale.}
\usage{
dist_gamma(shape, scale)
}
\arguments{
\item{shape}{Shape parameter k; must be finite and positive.}

\item{scale}{Scale parameter θ; must be finite and positive.}
}
\value{
A \code{Distribution} object.
}
\description{
Uses the shape–scale (k, θ) parameterization: the mean is \code{shape * scale} and
the variance is \code{shape * scale^2}. Draws are strictly positive, which makes the
gamma a natural choice for right-skewed, always-positive durations.
}
\examples{
d <- dist_gamma(2, 3)   # mean 6, variance 18
d$sample_one()
}
