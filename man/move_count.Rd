% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{move_count}
\alias{move_count}
\title{Apply a per-node transition count to the census at \code{tick + 1}.}
\usage{
move_count(from, to, counts, tick)
}
\arguments{
\item{from}{A 2-D census \link{Column} to decrement, or \code{NULL}.}

\item{to}{A 2-D census \link{Column} to increment, or \code{NULL}.}

\item{counts}{Integer vector of per-node counts (length \code{n_nodes}).}

\item{tick}{0-based source tick; the delta is applied at column \code{tick + 1}.}
}
\value{
\code{NULL}, invisibly; the Columns are modified in place.
}
\description{
Subtracts \code{counts} from the \code{from} compartment and adds it to the \code{to} compartment at
census column \code{tick + 1} (the working column the model has already carried forward).
Either side may be \code{NULL} to skip it — e.g. a death is a one-sided decrement
(\code{to = NULL}) and a birth a one-sided increment (\code{from = NULL}).
}
\examples{
\dontrun{
inf <- transmission(state, timer, nodeid, count, nodes$foi, t, states[["E"]], inc_dur)
move_count(nodes$S, nodes$E, inf, t)   # S -> E
}
}
