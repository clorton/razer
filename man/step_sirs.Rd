% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_sirs}
\alias{step_sirs}
\title{Advance M→S, E→I, I→R (with waning immunity), and R→S for one tick — SIRS / SEIRS.}
\usage{
step_sirs(state, timer, nodeid, count, n_nodes, inf_duration, imm_duration)
}
\arguments{
\item{state, timer, nodeid, count, n_nodes}{As in \code{\link[=step_si]{step_si()}}.}

\item{inf_duration}{A Distribution for the infectious period set on E→I.}

\item{imm_duration}{A Distribution for the immunity period set on I→R.}
}
\value{
\code{list(waned, onset, recovered, waned_r)} of \code{integer[n_nodes]}.
}
\description{
On I→R a fresh immunity timer is drawn from \code{imm_duration}; R→S fires when it expires.
Returns \code{list(waned, onset, recovered, waned_r)} of per-node counts.
}
