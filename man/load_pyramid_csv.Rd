% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyramid.R
\name{load_pyramid_csv}
\alias{load_pyramid_csv}
\title{Load a population-pyramid CSV into a numeric matrix.}
\usage{
load_pyramid_csv(file)
}
\arguments{
\item{file}{Path to the CSV file.}
}
\value{
An integer matrix with columns \code{start}, \code{end}, \code{M}, \code{F} (one row per band).
}
\description{
The file is expected to have the schema used by laser.core pyramids:
}
\details{
\itemize{
\item a header line exactly \code{"Age,M,F"};
\item then one line per age band \code{"low-high,males,females"} (all non-negative integers);
\item a final open-ended band \code{"max+,males,females"}.
}

The returned matrix has one row per band and the integer columns \code{start}, \code{end},
\code{M}, \code{F}. The final \code{"max+"} band is stored as a single-year bucket (\code{end == start}),
matching laser.core.
}
\section{Errors}{

Stops if the header is not \code{"Age,M,F"}, if any data line is malformed, or if the
start/end ages are not strictly ascending.
}

\examples{
\dontrun{
pyramid <- load_pyramid_csv("USA_pyramid_2020.csv")
}
}
