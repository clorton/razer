% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bincount.R
\name{bincount_where}
\alias{bincount_where}
\title{Count, per group, the agents whose property satisfies a comparison.}
\usage{
bincount_where(
  group,
  n_groups,
  prop,
  op,
  value,
  count,
  counts = NULL,
  slot = 0L
)
}
\arguments{
\item{group}{An integer-typed \link{Column} of group indices (\code{i8}..\code{u32}), each in
\verb{0..n_groups} — typically \code{nodeid}.}

\item{n_groups}{Number of groups; a non-negative integer.}

\item{prop}{A numeric \link{Column} holding the per-agent property to test
(compared as a double).}

\item{op}{Comparison string: one of \code{"eq"}, \code{"ne"}, \code{"lt"}, \code{"le"}, \code{"gt"},
\code{"ge"}.}

\item{value}{The threshold the property is compared against.}

\item{count}{How many leading agents to scan — the ACTIVE agent count, typically
\code{people$count}. Required: there is no implicit population size, and the Column may be
over-allocated (\code{capacity > count}), so scanning its full length would tally reserved,
inactive slots (which default to node 0 / state S). Pass the active count explicitly.}

\item{counts}{Optional numeric \link{Column} to receive the totals; when omitted an
integer result vector is allocated and returned instead.}

\item{slot}{Which slice of \code{counts} to write when \code{counts} is supplied; a
non-negative integer less than \code{counts}'s slice count. Defaults to \code{0}.}
}
\value{
When \code{counts} is \code{NULL}, an integer vector of per-group counts; otherwise
\code{NULL} invisibly (the result is written into \code{counts}).
}
\description{
A predicate-filtered, count-aware \code{\link[=bincount]{bincount()}}: for each group \code{g} in
\verb{0..n_groups}, counts how many of the first \code{count} agents both have
\code{group[i] == g} AND satisfy \verb{prop[i] <op> value}. This answers flexible
agent queries directly on the Columns — e.g. "exposed by node"
(\code{prop = state}, \code{op = "eq"}, \code{value = laser_states()[["E"]]}) or "under-fives
by node" (\code{prop = dob}, \code{op = "gt"}, \code{value = tick - 5 * 365}, since \code{dob} is the
negative age) — in one parallel pass with no copy of \code{prop} into R.
}
\details{
Two output modes: leave \code{counts} \code{NULL} (the default) for an ad-hoc query and an
integer vector of length \code{n_groups} is allocated and returned; or pass a numeric
\link{Column} \code{counts} (e.g. a \verb{n_ticks x n_nodes} report) and the totals are written
into its slice \code{slot} (and \code{NULL} returned invisibly), avoiding an allocation in a
per-tick model loop.
}
\examples{
states <- laser_states()
state  <- allocate_scalar("u8",  6L); state$set(c(0, 1, 1, 2, 1, 0))   # S E E I E S
nodeid <- allocate_scalar("u16", 6L); nodeid$set(c(0, 0, 1, 1, 1, 0))
bincount_where(nodeid, 2L, state, "eq", states[["E"]], count = 6L)   # exposed per node: 1 2
}
\seealso{
\code{\link[=bincount]{bincount()}}, \code{\link[=bincount_wt]{bincount_wt()}}, \code{\link[=bincount_where_wt]{bincount_where_wt()}}.
}
