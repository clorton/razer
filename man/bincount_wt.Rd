% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bincount.R
\name{bincount_wt}
\alias{bincount_wt}
\title{Weighted bincount: sum each element's weight into its bin.}
\usage{
bincount_wt(values, weights, nbins, counts, slot = 0L)
}
\arguments{
\item{values}{An integer-typed \link{Column} of bin indices (\code{i8}..\code{u32}).}

\item{weights}{A numeric \link{Column}, the same length as \code{values}.}

\item{nbins}{Number of bins; a non-negative integer no greater than \code{counts}'s
slice length.}

\item{counts}{A numeric \link{Column} that receives the weighted sums; modified in
place.}

\item{slot}{Which slice of \code{counts} to write; a non-negative integer less
than \code{counts}'s slice count. Defaults to \code{0}.}
}
\value{
\code{NULL}, invisibly; the result is written into \code{counts}.
}
\description{
Like \code{\link[=bincount]{bincount()}}, but accumulates \code{weights[i]} (rather than 1) into the bin
\code{values[i]}, à la \code{numpy.bincount(values, weights = ...)}. The per-bin sums are
written into \strong{slice \code{slot}} of \code{counts}. \code{weights} must be the same length as
\code{values} and may be any numeric \link{Column} (signed, unsigned, or floating point).
}
\examples{
values  <- allocate_scalar("u16", 5L); values$set(c(0, 0, 1, 2, 2))
weights <- allocate_scalar("f64", 5L); weights$set(c(1.5, 2.5, 4, 1, 3))
counts  <- allocate_scalar("f64", 3L)
bincount_wt(values, weights, 3L, counts)
counts$values()   # 4 4 4
}
\seealso{
\code{\link[=bincount]{bincount()}}, \code{\link[=bincount_where]{bincount_where()}}, \code{\link[=bincount_where_wt]{bincount_where_wt()}}.
}
