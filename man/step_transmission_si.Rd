% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_transmission_si}
\alias{step_transmission_si}
\title{Stochastic S→I transmission step (SI / SIR kernel).}
\usage{
step_transmission_si(people, nodes, beta, inf_duration)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{nodes}{LaserFrame of patches/nodes.}

\item{beta}{Transmission rate (force of infection per infectious contact per tick).}

\item{inf_duration}{Infectious period in ticks; written to \code{timer} on infection.}
}
\description{
\strong{Parallelism:} the active agent array is split into fixed chunks — one per
Rayon worker thread — matching the \code{prange} pattern. The aggregation phase
uses a thread-local fold then reduces by element-wise summation. The FOI
phase runs each chunk independently; RNG is thread-local (\code{rand::thread_rng()}).
}
\details{
All operations are performed in-place on the backing arrays; no copies are made.

Node-level I counts are computed from current agent states and written to
\code{nodes$I} (overwriting the previous value).

\strong{Required people properties:} \code{state} (integer), \code{node} (integer, 0-based),
\code{timer} (integer).
\strong{Required nodes properties:} \code{N} (integer, total population per node),
\code{I} (integer, will be overwritten).
}
