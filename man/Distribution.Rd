% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\docType{data}
\name{Distribution}
\alias{Distribution}
\alias{$.Distribution}
\title{A parameterized probability distribution that can be sampled repeatedly.}
\format{
An object of class \code{environment} of length 2.
}
\usage{
Distribution
}
\description{
Build one with a family constructor such as \code{dist_normal()} or \code{dist_constant()},
then either sample it from Rust via \code{sample()} (Pattern B: the caller owns the
RNG and passes it in), or from R via \verb{$sample_one()} / \verb{$sample_n()}.
}
\details{
Draws are always floating-point. Callers that need integer values (e.g. a
whole-tick state timer) are responsible for rounding or truncating as
appropriate — the simulation kernels round to the nearest tick.

The handle is opaque to R — it is passed to simulation kernels (e.g.
\code{step_sir} or \code{transmission}) by reference, so the same object can be reused
every tick and shared across all worker threads.
}
\section{Methods}{

\subsection{Method \code{sample_one}}{
Draw a single sample using a thread-local RNG.

Convenience for interactive use. Simulation kernels do not call this — they
use the internal sampler with an explicit, reusable RNG for performance.

\subsection{return}{
A single numeric (double) draw from the distribution.
}
}

\subsection{Method \code{sample_n}}{
Draw \code{n} samples using a thread-local RNG, returned as a numeric vector.

Drawing a whole batch in one call avoids per-sample R↔Rust overhead, which
makes it practical to validate the sampler against large empirical samples
(e.g. one million draws) from R.

\subsection{Arguments}{
\describe{
\item{\code{n}}{Number of samples to draw; must be non-negative.}
}}
\subsection{return}{
A numeric (double) vector of length \code{n}.
}
}
}

\keyword{datasets}
