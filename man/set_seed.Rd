% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{set_seed}
\alias{set_seed}
\title{Set the global random seed, making subsequent razer runs reproducible.}
\usage{
set_seed(seed)
}
\arguments{
\item{seed}{A finite, non-negative number (used as a 64-bit seed).}
}
\value{
\code{NULL}, invisibly.
}
\description{
After \code{set_seed(s)}, every kernel's randomness is a deterministic function of \code{s} and
the order of kernel calls — identical on every run and every machine, regardless of
CPU/thread count. Call it once at the start of a script. \code{unset_seed()} reverts to a
fresh (entropy-seeded) RNG.
}
\examples{
set_seed(42)
}
