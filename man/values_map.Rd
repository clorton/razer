% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values_map.R
\name{values_map}
\alias{values_map}
\title{Build a values map: broadcast a value into a \verb{n_ticks x n_nodes} grid \link{Column}.}
\usage{
values_map(value, n_ticks, n_nodes)
}
\arguments{
\item{value}{A scalar, a length-\code{n_nodes} or length-\code{n_ticks} numeric vector,
or a \verb{n_ticks x n_nodes} numeric matrix.}

\item{n_ticks}{Number of time slices (rows of the grid).}

\item{n_nodes}{Number of nodes (columns of the grid).}
}
\value{
An f64 \link{Column} of shape \verb{n_ticks x n_nodes}.
}
\description{
Expands a flexible per-time and/or per-node value into a full \verb{n_ticks x n_nodes}
f64 \link{Column} (slice-major: each tick's per-node row contiguous), suitable for
passing to \code{\link[=calc_foi]{calc_foi()}} as \code{beta} or \code{seasonality}. This is razer's equivalent of
LASER's \emph{ValuesMap}. The shape of \code{value} selects how it is broadcast:
}
\details{
\itemize{
\item \strong{scalar} (length 1) — constant over time and space.
\item \strong{length \code{n_nodes}} — varies by node, constant over time (per-node).
\item \strong{length \code{n_ticks}} — varies by time, constant over space (per-tick).
\item \strong{\verb{n_ticks x n_nodes} matrix} — varies by both; used as-is.
}

When \code{n_ticks == n_nodes} a bare vector is ambiguous; it is treated as per-node.
Pass an explicit matrix to vary by both in that case.
}
\examples{
g <- values_map(0.5, 10L, 3L)        # constant 0.5 everywhere
dim(g$values())                       # 10 3
values_map(c(1, 2, 3), 10L, 3L)       # per-node (length n_nodes)
values_map(seq_len(10L), 10L, 3L)     # per-tick (length n_ticks)
}
