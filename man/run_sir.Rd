% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{run_sir}
\alias{run_sir}
\title{Run an agent-based SIR model.}
\usage{
run_sir(nodes, beta, infectious_period, nticks, seed = 0L, progress = FALSE)
}
\arguments{
\item{nodes}{A data.frame with one row per geographic node and an integer
\code{population} column giving each node's initial population.}

\item{beta}{Transmission rate (force of infection per infectious contact per
tick).}

\item{infectious_period}{Infectious period in ticks: either a \code{Distribution}
(e.g. \code{dist_gamma(...)}) or a single number (a fixed period).}

\item{nticks}{Number of ticks to simulate (a positive integer).}

\item{seed}{Initial number of infectious agents per node: either one number
(applied to every node) or a vector of length \code{nrow(nodes)}. Defaults to 0
(no infections — supply a positive seed to start an epidemic).}

\item{progress}{If \code{TRUE}, draw a text progress bar during the run.}
}
\value{
The people \code{LaserFrame} (final per-agent state) with the recorded
trajectories and flows attached as attributes (see Details).
}
\description{
Builds a population of individual agents from a per-node scenario table, seeds
initial infections, and advances a stochastic SIR model for \code{nticks} ticks.
Each tick applies the kernels downstream-first — recovery (I→R) before
transmission (S→I) — so a newly infectious agent is not decremented in the
same tick it is infected (see the modeling-convention notes in \code{CLAUDE.md}).
}
\details{
The returned people \code{LaserFrame} holds the \emph{final} per-agent state. The
recorded time series are attached as attributes:
\itemize{
\item \code{attr(model, "report")} — a node-level \code{LaserFrame} (one row per node) whose
vector properties are the recorded matrices, each \code{n_nodes} rows by time:
\itemize{
\item \code{S}, \code{I}, \code{R}: compartment counts at every tick boundary (\code{nticks + 1}
columns; column 1 is the initial state, column \code{t + 1} is the state after
tick \code{t}).
\item \code{incidence} (S→I) and \code{recovery} (I→R): per-tick flow counts (\code{nticks}
columns; column \code{t} covers tick \code{t}).
}

Pull a matrix with \code{attr(model, "report")$I} (counts) or transpose with
\code{t(...)} to get time-down-the-rows.
\item \code{attr(model, "runtime")} — wall-clock seconds spent in the tick loop.
\item \code{attr(model, "nodes")} — the node frame passed to the transmission kernel.
\item \code{attr(model, "nticks")}, \code{attr(model, "model")}, \code{attr(model, "parameters")}.
}
}
\examples{
nodes <- data.frame(population = c(10000, 5000))
model <- run_sir(nodes, beta = 0.3, infectious_period = 7, nticks = 160,
                 seed = c(10, 0))
report <- attr(model, "report")
tail(t(report$I))        # infectious count over time, per node
colSums(report$incidence)  # total new infections per node
attr(model, "runtime")
}
