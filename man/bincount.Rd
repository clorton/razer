% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bincount.R
\name{bincount}
\alias{bincount}
\title{Count occurrences of each value, NumPy \code{bincount}-style, into a buffer.}
\usage{
bincount(values, nbins, counts, slot = 0L)
}
\arguments{
\item{values}{An integer-typed \link{Column} of bin indices (\code{i8}..\code{u32}), each in
\verb{0..nbins}.}

\item{nbins}{Number of bins; a non-negative integer no greater than \code{counts}'s
slice length.}

\item{counts}{A numeric \link{Column} that receives the counts; modified in place.}

\item{slot}{Which slice of \code{counts} to write; a non-negative integer less than
\code{counts}'s slice count. Defaults to \code{0}.}
}
\value{
\code{NULL}, invisibly; the result is written into \code{counts}.
}
\description{
For each bin \code{b} in \verb{0..nbins}, counts how many elements of \code{values} equal \code{b}
and writes the result into \strong{slice \code{slot}} of \code{counts} — the \code{nbins} entries
\verb{slot*slice_len .. slot*slice_len + nbins} — overwriting them and leaving the
rest untouched. For a scalar \code{counts} (shape \verb{(1, n)}) the only slice is
\code{slot = 0}, the whole vector; for a 2-D report (e.g. \verb{n_ticks x n_nodes} from
\code{\link[=allocate_vector]{allocate_vector()}}) \code{slot} selects a tick's row. The tally is parallelized with
private per-thread histograms, so there are no write collisions.
}
\examples{
values <- allocate_scalar("u16", 6L)
values$set(c(0, 1, 1, 2, 2, 2))
counts <- allocate_scalar("i32", 3L)
bincount(values, 3L, counts)
counts$values()   # 1 2 3
}
\seealso{
\code{\link[=bincount_wt]{bincount_wt()}}, \code{\link[=bincount_where]{bincount_where()}}, \code{\link[=bincount_where_wt]{bincount_where_wt()}}.
}
