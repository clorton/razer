% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{bincount}
\alias{bincount}
\title{Count occurrences of each value, NumPy \code{bincount}-style, into a buffer.}
\usage{
bincount(values, nbins, counts)
}
\arguments{
\item{values}{An integer-typed \code{Column} of bin indices (\code{i8}..\code{u32}).}

\item{nbins}{Number of bins; a non-negative integer \verb{<= counts$length()}.}

\item{counts}{A numeric \code{Column} (length \verb{>= nbins}) that receives the counts.
It is modified in place; the function returns \code{NULL}.}
}
\value{
\code{NULL} (invisibly); the result is written into \code{counts}.
}
\description{
For each bin \code{b} in \verb{0..nbins}, computes how many elements of \code{values} equal
\code{b} and writes that into \code{counts[b]}. \code{values} must be an integer-typed
\link{Column} whose elements lie in \verb{0..nbins} (they are used as bin indices);
\code{counts} must be a numeric \link{Column} with at least \code{nbins} elements and is
\strong{overwritten} in its first \code{nbins} entries (entries at or beyond \code{nbins} are
left untouched). The work is parallelized with private per-thread histograms,
so there are no write collisions.
}
\examples{
values <- allocate_scalar("u16", 6L)
values$set(c(0, 1, 1, 2, 2, 2))
counts <- allocate_scalar("i32", 3L)
bincount(values, 3L, counts)
counts$values()   # 1 2 3
}
