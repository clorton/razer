% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_timer_expire}
\alias{step_timer_expire}
\title{Generalized timer-expiry transition into an \emph{absorbing} (untimed) state.}
\usage{
step_timer_expire(people, from_state, to_state)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{from_state}{Integer state code an agent must currently occupy to be eligible.}

\item{to_state}{Integer state code an agent moves to when its timer expires.}
}
\description{
For each agent currently in \code{from_state}, decrements \code{timer} by 1; when the
timer reaches 0 the agent moves to \code{to_state} and its timer is left at 0. The
destination carries no duration of its own — this is the "transition to an
absorbing state" generalization (laser-generic's \code{nb_timer_update}), e.g.
I→S in SIS, I→R in SIR, or R→S waning.
}
\details{
This is the engine behind \code{\link[=step_infectious_is]{step_infectious_is()}} (I→S) and
\code{\link[=step_recovered_rs]{step_recovered_rs()}} (R→S):
\code{step_timer_expire(people, laser_states()[["I"]], laser_states()[["S"]])}
is exactly \code{step_infectious_is(people)}.

\strong{Required people properties:} \code{state}, \code{timer} (both integer).
}
