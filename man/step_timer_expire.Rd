% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_timer_expire}
\alias{step_timer_expire}
\title{Generic timed transition \code{from_state -> to_state} into an UNTIMED destination.}
\usage{
step_timer_expire(state, timer, nodeid, count, n_nodes, from_state, to_state)
}
\arguments{
\item{state, timer, nodeid, count, n_nodes}{As in \code{\link[=step_si]{step_si()}}.}

\item{from_state}{Integer state code an agent must occupy to be eligible.}

\item{to_state}{Integer (untimed) state code an agent moves to on expiry.}
}
\value{
An integer vector of per-node transition counts (length \code{n_nodes}).
}
\description{
For each agent in \code{from_state}, decrements its u16 \code{timer}; on expiry the agent moves
to \code{to_state} (timer left at 0). Returns the per-node count of transitions. Compose
these (downstream-first) to build models beyond the named menagerie; apply the counts
with \code{move_count}. Generalizes the M→S / R→S / I→S or I→R legs.
}
