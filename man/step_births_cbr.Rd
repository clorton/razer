% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_births_cbr}
\alias{step_births_cbr}
\title{Stochastic birth step using a crude birth rate.}
\usage{
step_births_cbr(people, cbr)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{cbr}{Crude birth rate per agent per tick (probability in [0, 1]).}
}
\description{
\strong{Parallelism:} the Bernoulli draw (does this agent give birth?) is
parallelised across all active agents. The subsequent bookkeeping —
incrementing \code{count} and writing new-agent properties — is serial because
it modifies the frame's active count.
}
\details{
For each active (non-D) agent, draws Bernoulli(\code{cbr}) for a birth event.
Each birth creates one new agent that inherits the parent's \code{node} and
starts in state S with \code{timer = 0}. Other scalar properties of new agents
take the default value set at \code{add_scalar_property} time.

Excess births beyond \code{capacity - count} are silently dropped.

\strong{Required people properties:} \code{state}, \code{node}, \code{timer} (all integer).
}
