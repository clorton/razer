% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_mortality_cdr}
\alias{step_mortality_cdr}
\title{Stochastic mortality step using a crude death rate.}
\usage{
step_mortality_cdr(people, cdr)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{cdr}{Crude death rate per agent per tick (probability in [0, 1]).}
}
\description{
For each living (non-D) agent, draws Bernoulli(\code{cdr}) for death.
Agents that die have their state set to D (-1) and \code{timer} set to 0.
Dead agents remain in the frame — call \verb{$squash(people$state >= 0L)}
periodically to compact.
}
\details{
Each agent's draw is independent; Rayon assigns a fixed slice to each worker
thread. RNG is thread-local (no locking).
}
