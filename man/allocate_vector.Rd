% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{allocate_vector}
\alias{allocate_vector}
\title{Allocate a fresh, zero-filled 2-D property array (a per-slot report buffer).}
\usage{
allocate_vector(dtype, n_slices, slice_len)
}
\arguments{
\item{dtype}{Element type (see \code{\link[=allocate_scalar]{allocate_scalar()}} for the accepted names).}

\item{n_slices}{Number of slices — the first/outer dimension (e.g. the tick
count). A non-negative integer.}

\item{slice_len}{Contiguous length of each slice — the inner dimension (e.g. the
node count). A non-negative integer.}
}
\value{
A \link{Column} of shape \verb{n_slices × slice_len}, all elements zero.
}
\description{
Returns an opaque \link{Column} of \code{n_slices * slice_len} elements laid out
SLICE-MAJOR (row-major): \code{n_slices} slices, each a contiguous run of \code{slice_len}
elements. Slice \code{s} is the block \verb{s*slice_len .. (s+1)*slice_len}, so indexing
the FIRST dimension yields a contiguous array. The conventional use is a
time-series report with the \strong{first dimension time and the second node} —
\code{allocate_vector(dtype, n_ticks, n_nodes)} — so each tick's per-node values are
contiguous (cache-friendly for the step kernels that fill one tick at a time).
\verb{$values()} reads it back as an \verb{n_slices × slice_len} (e.g. \verb{n_ticks × n_nodes})
R matrix, so row \code{t} is tick \code{t}'s vector.
}
\examples{
recoveries <- allocate_vector("u32", 4L, 3L)   # 4 ticks x 3 nodes
dim(recoveries$values())                        # 4 3
}
