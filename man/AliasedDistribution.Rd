% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\docType{data}
\name{AliasedDistribution}
\alias{AliasedDistribution}
\alias{$.AliasedDistribution}
\title{A discrete distribution over bin indices \verb{0..n}, sampled by the Vose alias method.}
\format{
An object of class \code{environment} of length 6.
}
\usage{
AliasedDistribution
}
\description{
Construct with \code{\link[=aliased_distribution]{aliased_distribution()}} from a vector of non-negative counts; the
probability of drawing bin \code{i} is \code{counts[i] / sum(counts)}. Draws are 0-based bin
indices. The handle is opaque to R.
}
\section{Methods}{

\subsection{Method \code{sample_one}}{
Draw a single bin index (0-based) using a thread-local RNG.

\subsection{return}{
A single integer bin index in \verb{0..n_bins()}.
}
}

\subsection{Method \code{sample_n}}{
Draw \code{n} bin indices (0-based), returned as an integer vector.

The draws are split across CPU cores (each with its own thread-local RNG), so
generating a national-scale population in one call is cheap.

\subsection{Arguments}{
\describe{
\item{\code{n}}{Number of samples to draw; must be non-negative.}
}}
\subsection{return}{
An integer vector of length \code{n} of bin indices in \verb{0..n_bins()}.
}
}

\subsection{Method \code{n_bins}}{
The number of bins.
\subsection{return}{
An integer.
}
}

\subsection{Method \code{total}}{
The total weight (sum of the original counts).
\subsection{return}{
A numeric scalar (the sum may exceed R's integer range).
}
}

\subsection{Method \code{alias}}{
The alias table (0-based partner bin per bin, or -1 for none). For inspection.
\subsection{return}{
An integer vector of length \code{n_bins()}.
}
}

\subsection{Method \code{probs}}{
The per-bin own-mass thresholds (out of \code{total()}). For inspection.
\subsection{return}{
A numeric vector of length \code{n_bins()}.
}
}
}

\keyword{datasets}
