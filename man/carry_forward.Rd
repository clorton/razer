% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{carry_forward}
\alias{carry_forward}
\title{Carry a per-node counter forward one tick: copy column \code{tick} onto \code{tick + 1}.}
\usage{
carry_forward(counter, tick)
}
\arguments{
\item{counter}{A 2-D Column to carry forward.}

\item{tick}{0-based source tick; column \code{tick} is copied onto column \code{tick+1}.}
}
\value{
\code{NULL} (invisibly); \code{counter} is modified in place.
}
\description{
For a 2-D report \link{Column} (e.g. \verb{n_ticks+1 × n_nodes}), copies the contiguous
slice for \code{tick} onto the slice for \code{tick + 1}. This seeds the next tick with the
current counts so a dynamics kernel can then update it in place — keeping the
census invariant \verb{count[t+1] = count[t] ± delta}. Call it once per state that
must persist across ticks (e.g. S, I, R for an SIR model; add E for SEIR, or a
user-defined "V" vaccinated count). Works for any element type.
}
\examples{
counts <- allocate_vector("i32", 3L, 2L)   # 3 ticks x 2 nodes
counts$set(c(5L, 7L, 0L, 0L, 0L, 0L))      # tick 0 = (5, 7)
carry_forward(counts, 0L)
counts$values()[2L, ]                       # 5 7  (carried to tick 1)
}
