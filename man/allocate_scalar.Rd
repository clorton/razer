% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{allocate_scalar}
\alias{allocate_scalar}
\title{Allocate a fresh, zero-filled property array of a given type and length.}
\usage{
allocate_scalar(dtype, count)
}
\arguments{
\item{dtype}{Element type, one of \code{"i8"}, \code{"u8"}, \code{"i16"}, \code{"u16"}, \code{"i32"},
\code{"u32"}, \code{"f32"}, \code{"f64"} (aliases: \code{"int8"}, \code{"uint8"}/\code{"raw"}, …,
\code{"integer"} = i32, \code{"double"}/\code{"real"} = f64, \code{"single"} = f32).}

\item{count}{Array length (number of elements); a non-negative integer.}
}
\value{
A \link{Column} object whose elements are all zero.
}
\description{
Returns an opaque \link{Column} handle backed by a Rust-owned buffer. Choose the
narrowest type that fits the data to minimize memory (e.g. \code{"u8"} for a small
set of disease-state codes).
}
\examples{
state <- allocate_scalar("u8", 5L)
state$dtype()    # "u8"
state$length()   # 5
state$values()   # 0 0 0 0 0
}
