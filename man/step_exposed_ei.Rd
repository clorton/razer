% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_exposed_ei}
\alias{step_exposed_ei}
\title{Timer-based E→I transition (SEIR kernel).}
\usage{
step_exposed_ei(people, inf_dist)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{inf_dist}{A \code{Distribution} (e.g. from \code{dist_constant()} or \code{dist_normal()})
giving the infectious period in ticks; sampled and written to \code{timer} on E→I.}
}
\description{
For each exposed agent, decrements \code{timer} by 1. When \code{timer} reaches 0 the
agent transitions to state I and \code{timer} is set to a fresh draw from
\code{inf_dist}, the infectious-period distribution. Pass \code{dist_constant(d)} for a
fixed period of \code{d} ticks, or e.g. \code{dist_normal(mean, variance)} for a stochastic
per-agent period. Draws are rounded to the nearest tick and clamped to a
minimum of 1.
}
\details{
\strong{RNG:} thread-local — each Rayon worker draws from its own \code{thread_rng}
(Pattern B: the kernel owns the RNG and passes it into the sampler). The
single \code{inf_dist} handle is shared across threads by reference.
}
