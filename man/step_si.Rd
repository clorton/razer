% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_si}
\alias{step_si}
\title{Advance M→S (maternal waning) and E→I (incubation) for one tick — SI / SEI.}
\usage{
step_si(state, timer, nodeid, count, n_nodes, inf_duration)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (mutated).}

\item{timer}{Per-agent \code{u16} timer Column (mutated; E→I draws an infectious timer).}

\item{nodeid}{Per-agent \code{u16} 0-based node-id Column.}

\item{count}{Number of active agents to process.}

\item{n_nodes}{Number of nodes (the length of each returned vector).}

\item{inf_duration}{A Distribution for the infectious period set on E→I.}
}
\value{
\code{list(waned = integer[n_nodes], onset = integer[n_nodes])}.
}
\description{
\code{I} is terminal (no exit). Returns \code{list(waned, onset)} of per-node counts.
}
