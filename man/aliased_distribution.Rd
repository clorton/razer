% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{aliased_distribution}
\alias{aliased_distribution}
\title{Build an \link{AliasedDistribution} from a vector of non-negative bin counts.}
\usage{
aliased_distribution(counts)
}
\arguments{
\item{counts}{A numeric vector of non-negative per-bin counts (length >= 1).}
}
\value{
An \code{AliasedDistribution} object.
}
\description{
The probability of drawing bin \code{i} (0-based) is \code{counts[i] / sum(counts)}. Counts
are rounded to whole numbers; they must be finite and non-negative and sum to a
positive total. A typical use is the per-age-bin population of a demographic
pyramid (e.g. males + females in each five-year band).
}
\examples{
d <- aliased_distribution(c(10, 30, 60))   # bin 2 drawn ~60\% of the time
table(d$sample_n(10000L))                  # 0-based bin indices
}
