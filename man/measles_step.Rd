% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{measles_step}
\alias{measles_step}
\title{Advance the measles timed compartments for one tick (M→S, E→I, I→R).}
\usage{
measles_step(
  state,
  timer,
  nodeid,
  count,
  m_count,
  s_count,
  e_count,
  i_count,
  r_count,
  inf_duration,
  tick
)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (mutated).}

\item{timer}{Per-agent \code{u16} countdown Column (mutated; decremented, reset on E→I).}

\item{nodeid}{Per-agent \code{u16} 0-based node-id Column.}

\item{count}{Number of active agents to process.}

\item{m_count, s_count, e_count, i_count, r_count}{\verb{n_ticks x n_nodes} i32 census
Columns kept in sync (mutated at column \code{tick + 1}).}

\item{inf_duration}{A \code{Distribution} for the infectious period drawn on E→I.}

\item{tick}{0-based tick index.}
}
\value{
\code{NULL} (invisibly); the Columns are modified in place.
}
\description{
For each of the first \code{count} agents, decrements its \code{timer} (uint16) and, on expiry,
transitions it: M→S (timer left at 0), E→I (timer set to a fresh draw from
\code{inf_duration}, the infectious period), or I→R (timer left at 0). Each agent is in at
most one timed state, so it is processed at most once. The per-compartment census
deltas are applied at column \code{tick + 1} (the working columns the caller has already
carried forward): M and E lose their leavers, R gains its arrivals, S gains the M→S
waners, and I gains the E→I arrivals minus the I→R leavers. Parallelized across cores
with private per-thread node buffers reduced at the end.
}
