% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_timer_expire_set}
\alias{step_timer_expire_set}
\title{Generic timed transition \code{from_state -> to_state} into a TIMED destination.}
\usage{
step_timer_expire_set(
  state,
  timer,
  nodeid,
  count,
  n_nodes,
  from_state,
  to_state,
  duration
)
}
\arguments{
\item{state, timer, nodeid, count, n_nodes}{As in \code{\link[=step_si]{step_si()}}.}

\item{from_state}{Integer state code an agent must occupy to be eligible.}

\item{to_state}{Integer state code an agent moves to on expiry.}

\item{duration}{A Distribution for the destination state's timer.}
}
\value{
An integer vector of per-node transition counts (length \code{n_nodes}).
}
\description{
Like \code{\link[=step_timer_expire]{step_timer_expire()}} but on expiry the agent's \code{timer} is reset to a fresh draw
from \code{duration} (the destination state's own clock — e.g. E→I sets the infectious
period, I→R sets a waning-immunity period). Returns per-node transition counts.
}
