% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_timer_expire_set}
\alias{step_timer_expire_set}
\title{Generalized timer-expiry transition into a state that has \emph{its own} duration.}
\usage{
step_timer_expire_set(people, from_state, to_state, duration_dist)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{from_state}{Integer state code an agent must currently occupy to be eligible.}

\item{to_state}{Integer state code an agent moves to when its timer expires.}

\item{duration_dist}{A \code{Distribution} (e.g. \code{dist_constant()} or \code{dist_normal()})
giving the destination state's duration in ticks; sampled per transitioning
agent and written to \code{timer}.}
}
\description{
For each agent currently in \code{from_state}, decrements \code{timer} by 1; when the
timer reaches 0 the agent moves to \code{to_state} and \code{timer} is reset to a fresh
per-agent draw from \code{duration_dist} (rounded to whole ticks, clamped to a
minimum of 1). This is the "transition to a state with its own duration timer"
generalization (laser-generic's \code{nb_timer_update_timer_set}), e.g. E→I in
SEIR or I→R with waning in SEIRS.
}
\details{
This is the engine behind \code{\link[=step_exposed_ei]{step_exposed_ei()}} (E→I) and
\code{\link[=step_infectious_ir]{step_infectious_ir()}} (I→R):
\code{step_timer_expire_set(people, laser_states()[["E"]], laser_states()[["I"]], inf_dist)}
is exactly \code{step_exposed_ei(people, inf_dist)}.

\strong{RNG:} thread-local (Pattern B) — each Rayon worker draws from its own
\code{thread_rng}; the single \code{duration_dist} handle is shared across threads by
reference.

\strong{Required people properties:} \code{state}, \code{timer} (both integer).
}
