% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{stouffer}
\alias{stouffer}
\title{Stouffer's intervening-opportunities migration model (Stouffer, 1940).}
\usage{
stouffer(pops, distances, k, a, b, include_home)
}
\arguments{
\item{pops}{Numeric vector of node populations (length N, non-negative).}

\item{distances}{Symmetric \verb{N × N} numeric distance matrix.}

\item{k}{Scaling constant for the flow magnitude (non-negative).}

\item{a}{Exponent on the origin population.}

\item{b}{Exponent on the destination/cumulative-population ratio.}

\item{include_home}{Logical; whether the home population is included in the
cumulative sum.}
}
\value{
An \verb{N × N} numeric migration-weight matrix (generally not symmetric).
}
\description{
For each source \code{i} and destination \code{j},
\code{network[i, j] = k * p_i^a * (p_j / s)^b}, where \code{s} is the cumulative
population as close or closer to \code{i} than \code{j} (excluding the home population
when \code{include_home = FALSE}); the nearest node (the source) gets weight 0.
Port of laser-core's \code{stouffer}. The diagonal is 0.
}
