% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{step_transmission_se}
\alias{step_transmission_se}
\title{Stochastic S→E exposure step (SEIR kernel).}
\usage{
step_transmission_se(people, nodes, beta, exp_dist, network)
}
\arguments{
\item{people}{LaserFrame of agents.}

\item{nodes}{LaserFrame of patches/nodes.}

\item{beta}{Transmission rate.}

\item{exp_dist}{A \code{Distribution} (e.g. \code{dist_constant()} or \code{dist_normal()}) giving the
incubation period in ticks; sampled per newly exposed agent and written to
\code{timer} on S→E (rounded to whole ticks, clamped to a minimum of 1).}

\item{network}{An \verb{n_nodes × n_nodes} numeric matrix of exported
force-of-infection fractions (see \code{\link[=step_transmission_si]{step_transmission_si()}}); all-zero for
independent nodes.}
}
\description{
Same FOI computation and parallelism as \code{step_transmission_si}, but newly
exposed agents move to state E and \code{timer} is set to a draw from \code{exp_dist}
(incubation period). Pair with \code{step_exposed_ei} to complete E→I.
}
\details{
Spatial coupling via \code{network} works exactly as in \code{\link[=step_transmission_si]{step_transmission_si()}}:
an \verb{n_nodes × n_nodes} matrix redistributes the per-node force of infection
before agents are exposed. Use an all-zero matrix for independent nodes.

\strong{Required people properties:} \code{state}, \code{node}, \code{timer} (all integer).
\strong{Required nodes properties:} \code{N}, \code{I} (integer; \code{I} is overwritten).
}
