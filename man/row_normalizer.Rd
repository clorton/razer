% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{row_normalizer}
\alias{row_normalizer}
\title{Cap each row sum of a network matrix at \code{max_rowsum}.}
\usage{
row_normalizer(network, max_rowsum)
}
\arguments{
\item{network}{A square non-negative numeric matrix.}

\item{max_rowsum}{Maximum allowed row sum, in \verb{[0, 1]}.}
}
\value{
The row-capped matrix, same shape as \code{network}.
}
\description{
Rows whose total exceeds \code{max_rowsum} are scaled down proportionally so they
sum to exactly \code{max_rowsum}; smaller rows are left unchanged. Port of
laser-core's \code{row_normalizer}. Useful to bound the fraction of a node's force
of infection that is exported before passing the matrix as the transmission
\code{network}.
}
