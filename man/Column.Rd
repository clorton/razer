% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\docType{data}
\name{Column}
\alias{Column}
\alias{$.Column}
\title{A Rust-owned, dtype-tagged property array (1-D scalar, or a 2-D vector report).}
\format{
An object of class \code{environment} of length 7.
}
\usage{
Column
}
\description{
Allocate one with \code{\link[=allocate_scalar]{allocate_scalar()}} (\code{nrows} elements, \code{ncols == 1}) or
\code{\link[=allocate_vector]{allocate_vector()}} (\code{nrows}-per-slot × \code{ncols} slots, stored COLUMN-MAJOR so a
whole slot/time-slice is contiguous). The data is held in Rust and exposed to
R only as an opaque handle; use \verb{$values()} to copy a snapshot back into an R
vector (or matrix, when \code{ncols > 1}) for inspection, \verb{$fill()} / \verb{$set()} to
write, and \verb{$length()} / \verb{$dtype()} to query. The simulation step kernels
operate on the buffer in place with no copies.
}
\section{Methods}{

\subsection{Method \code{length}}{
Number of elements in the array.
\subsection{return}{
An integer length.
}
\subsection{export}{

}
}

\subsection{Method \code{dtype}}{
The element data type as a string (e.g. \code{"u8"}, \code{"f32"}).
\subsection{return}{
A length-1 character vector.
}
\subsection{export}{

}
}

\subsection{Method \code{values}}{
Copy the array into an R vector for inspection (NOT a view — a snapshot).

Integer-width types (i8, u8, i16, u16, i32) widen to R \code{integer}; \code{u32},
\code{f32}, and \code{f64} widen to R \code{double} (since \code{u32} overflows R's signed
32-bit integer). This O(n) copy is the only place data leaves Rust.

For a 2-D column (\code{n_slices > 1}, from \code{\link[=allocate_vector]{allocate_vector()}}) the result carries
a \code{dim} attribute and reads back as an \verb{n_slices × slice_len} R matrix (e.g.
\verb{n_ticks × n_nodes}), so row \code{t} is tick \code{t}'s per-node vector; otherwise a
plain vector. The snapshot is transposed during the copy (our buffer is
slice-major, R matrices are column-major) — inexpensive, inspection-only.

\subsection{return}{
A numeric vector (or matrix) — integer or double — of \code{length()} elements.
}
\subsection{export}{

}
}

\subsection{Method \code{fill}}{
Set every element to \code{value}, cast to the array's data type.

For integer-typed arrays the value is truncated toward zero (e.g. \code{2.9}
becomes \code{2}); out-of-range values wrap per Rust's \code{as} cast.

\subsection{Arguments}{
\describe{
\item{\code{value}}{A single numeric value to broadcast across the array.}
}}
\subsection{export}{

}
}

\subsection{Method \code{set}}{
Overwrite the array from an R numeric vector (integer or double).

The input length must equal \code{length()}. Each element is cast to the array's
data type (integer-typed arrays truncate toward zero). Useful for setup —
e.g. writing per-agent node assignments or seeding initial states from R.

\subsection{Arguments}{
\describe{
\item{\code{values}}{A numeric vector of length \code{length()}.}
}}
\subsection{export}{

}
}

\subsection{Method \code{col}}{
Read one column (\code{slot}) of a 2-D Column as an R vector snapshot.

Returns the \code{slice_len} values in column \code{slot} (e.g. all nodes for one tick),
widened to R \code{integer}/\code{double} like \code{\link[=values]{values()}}. For a scalar column the only
valid \code{slot} is 0 (the whole vector).

\subsection{Arguments}{
\describe{
\item{\code{slot}}{0-based column index, less than the number of columns.}
}}
\subsection{return}{
A numeric vector of length \code{slice_len}.
}
\subsection{export}{

}
}

\subsection{Method \code{set_col}}{
Write \code{values} into one column (\code{slot}) of a 2-D Column, in place.

\code{values} must have length \code{slice_len}; each element is cast to the column's
data type. Lets the caller update a single column (e.g. one tick's per-node
slice — a derived population total) without rewriting the whole buffer.

\subsection{Arguments}{
\describe{
\item{\code{slot}}{  0-based column index, less than the number of columns.}
\item{\code{values}}{A numeric vector of length \code{slice_len}.}
}}
\subsection{export}{

}
}
}

\keyword{datasets}
