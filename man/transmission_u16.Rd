% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{transmission_u16}
\alias{transmission_u16}
\title{Transmission step (S→\code{to_state}) writing a uint16 timer — the measles counterpart of
\code{\link[=transmission]{transmission()}}, which writes a u8 timer.}
\usage{
transmission_u16(
  state,
  timer,
  nodeid,
  count,
  foi,
  s_count,
  to_count,
  incidence,
  tick,
  to_state,
  duration
)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (mutated).}

\item{timer}{Per-agent \code{u16} countdown Column for the receiving state (mutated).}

\item{nodeid}{Per-agent \code{u16} 0-based node-id Column.}

\item{count}{Number of active agents to process.}

\item{foi}{\verb{n_ticks x n_nodes} f64 FOI Column (from \code{\link[=calc_foi]{calc_foi()}}); column \code{tick} read.}

\item{s_count}{i32 census Column for \code{S} (mutated at \code{tick+1}).}

\item{to_count}{i32 census Column for the receiving state (mutated at \code{tick+1}).}

\item{incidence}{i32 flow Column (mutated at \code{tick}).}

\item{tick}{0-based tick index.}

\item{to_state}{State code new cases enter (e.g. \code{laser_states()[["E"]]}).}

\item{duration}{A Distribution from which the receiving state's timer is drawn.}
}
\value{
\code{NULL} (invisibly); the Columns are modified in place.
}
\description{
Identical to \code{\link[=transmission]{transmission()}} in every respect except the \code{timer} Column is \code{u16}
(clamped to \verb{[1, 65535]}): converts column \code{tick} of \code{foi} to a per-node probability
\code{1 - exp(-foi)} once per node, then for each susceptible agent moves it (with that
probability) into \code{to_state} — \code{E} for measles — drawing its \code{timer} from \code{duration}
(the incubation period). New cases per node are applied as a delta to column \code{tick+1}
of the \code{s_count}/\code{to_count} census and recorded in column \code{tick} of \code{incidence}.
}
