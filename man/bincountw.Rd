% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{bincountw}
\alias{bincountw}
\title{Weighted bincount: sum each element's weight into its bin (NumPy
\code{bincount(values, weights=...)}).}
\usage{
bincountw(values, weights, nbins, counts)
}
\arguments{
\item{values}{An integer-typed \code{Column} of bin indices (\code{i8}..\code{u32}).}

\item{weights}{A numeric \code{Column} (any type), the same length as \code{values}.}

\item{nbins}{Number of bins; a non-negative integer \verb{<= counts$length()}.}

\item{counts}{A numeric \code{Column} (length \verb{>= nbins}) that receives the sums.
It is modified in place; the function returns \code{NULL}.}
}
\value{
\code{NULL} (invisibly); the result is written into \code{counts}.
}
\description{
For each bin \code{b} in \verb{0..nbins}, computes \verb{sum of weights[i] over all i with values[i] == b} and writes it into \code{counts[b]}. \code{values} must be an
integer-typed \link{Column} of bin indices in \verb{0..nbins}; \code{weights} is any numeric
\link{Column} (signed, unsigned, or floating point) the SAME length as \code{values};
\code{counts} is a numeric \link{Column} with at least \code{nbins} elements, \strong{overwritten}
in its first \code{nbins} entries (entries at or beyond \code{nbins} are left untouched).
Parallelized with private per-thread accumulators, so there are no write
collisions.
}
\examples{
values  <- allocate_scalar("u16", 5L); values$set(c(0, 0, 1, 2, 2))
weights <- allocate_scalar("f64", 5L); weights$set(c(1.5, 2.5, 4, 1, 3))
counts  <- allocate_scalar("f64", 3L)
bincountw(values, weights, 3L, counts)
counts$values()   # 4 4 4
}
