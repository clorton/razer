% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{radiation}
\alias{radiation}
\title{Radiation migration-network model (Simini et al., Nature 2012).}
\usage{
radiation(pops, distances, k, include_home)
}
\arguments{
\item{pops}{Numeric vector of node populations (length N, non-negative).}

\item{distances}{Symmetric \verb{N × N} numeric distance matrix.}

\item{k}{Scaling constant for the flow magnitude (non-negative).}

\item{include_home}{Logical; whether the home (source) population is included
in the "as close or closer" cumulative sum.}
}
\value{
An \verb{N × N} numeric migration-weight matrix (generally not symmetric).
}
\description{
For each source \code{i}, destinations are ranked by distance and the weight to
destination \code{j} is
\code{k * p_i * p_j / (p_i + s) / (p_i + p_j + s)}, where \code{s} is the total
population of all nodes as close or closer to \code{i} than \code{j} (excluding the home
population \code{p_i} when \code{include_home = FALSE}). Port of laser-core's
\code{radiation}. The diagonal is 0.
}
