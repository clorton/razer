% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{transmission_si}
\alias{transmission_si}
\title{Stochastic transmission S→I into an ABSORBING \code{I} (the SI model), returning new
infections per node.}
\usage{
transmission_si(state, nodeid, count, foi, tick)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (mutated; S→I).}

\item{nodeid}{Per-agent \code{u16} 0-based node-id Column.}

\item{count}{Number of active agents to process.}

\item{foi}{\verb{n_ticks x n_nodes} f64 FOI Column; column \code{tick} read.}

\item{tick}{0-based tick index.}
}
\value{
An integer vector of new infections per node (length \code{n_nodes}).
}
\description{
Like \code{\link[=transmission]{transmission()}} but the agent enters \code{I} permanently — no \code{timer} is set (\code{I}
is terminal in SI). Returns the per-node count of new infections; the caller applies
the \code{S} ↓ / \code{I} ↑ delta.
}
