% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyramid.R
\name{sample_pyramid_ages}
\alias{sample_pyramid_ages}
\title{Sample realistic per-agent ages (in days) from a population pyramid.}
\usage{
sample_pyramid_ages(pyramid, n)
}
\arguments{
\item{pyramid}{An integer matrix with columns \code{start}, \code{end}, \code{M}, \code{F}, as returned
by \code{\link[=load_pyramid_csv]{load_pyramid_csv()}}.}

\item{n}{Number of agent ages to draw.}
}
\value{
An integer vector of length \code{n} of ages in whole days.
}
\description{
Builds an \code{\link[=aliased_distribution]{aliased_distribution()}} over the per-band populations (\code{M + F}), draws a
band for each agent in proportion to its population, then a uniform day within that
band's year range \verb{[start, end + 1)} — so an agent in the \code{0-4} band gets an age
uniformly in \verb{[0, 5)} years.
}
\details{
Band selection uses the package's internal (thread-local, not R-seedable)
RNG via the alias sampler; the within-band day uses R's RNG (\code{set.seed}-able).
}
\examples{
pyramid <- rbind(c(0, 4, 100, 98), c(5, 9, 90, 92), c(10, 10, 5, 6))
colnames(pyramid) <- c("start", "end", "M", "F")
ages_days <- sample_pyramid_ages(pyramid, 1000L)
}
