% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carry_states.R
\name{carry_forward_states}
\alias{carry_forward_states}
\title{Carry census counters forward, and optionally total some of them.}
\usage{
carry_forward_states(carry, tick, total = NULL, summands = carry)
}
\arguments{
\item{carry}{A list of 2-D census \link{Column}s to carry forward.}

\item{tick}{0-based source tick; column \code{tick} is copied onto \code{tick + 1}.}

\item{total}{Optional \link{Column} to receive the running total at column \code{tick + 1}.}

\item{summands}{List of Columns to sum into \code{total} (defaults to \code{carry}).}
}
\value{
\code{NULL}, invisibly; the Columns are modified in place.
}
\description{
For each Column in \code{carry}, calls \code{\link[=carry_forward]{carry_forward()}} (copying column \code{tick} onto
\code{tick + 1}). If \code{total} is supplied, then after carrying it sets \code{total}'s column
\code{tick + 1} to the elementwise sum of the \code{summands} Columns at \code{tick + 1} — for
example carrying \code{S}, \code{I}, \code{R} forward and totalling them into \code{N} so the current
per-node population is available to \code{\link[=calc_foi]{calc_foi()}} (and stays correct as births,
deaths, and imports change the compartments).
}
\examples{
\dontrun{
# carry S, I, R forward and keep N = S + I + R up to date:
carry_forward_states(list(nodes$S, nodes$I, nodes$R), tick, total = nodes$N)
}
}
