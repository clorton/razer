% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{constant_pop_vitals_sir}
\alias{constant_pop_vitals_sir}
\title{Apply constant-population SIR vital dynamics for one tick.}
\usage{
constant_pop_vitals_sir(
  state,
  timer,
  nodeid,
  count,
  rate,
  s_count,
  i_count,
  r_count,
  births,
  deaths,
  tick
)
}
\arguments{
\item{state}{Per-agent \code{u8} state Column (mutated; deaths reset to Susceptible).}

\item{timer}{Per-agent \code{u8} countdown Column (mutated; deaths reset to 0).}

\item{nodeid}{Per-agent \code{u16} 0-based node-id Column.}

\item{count}{Number of active agents to process.}

\item{rate}{Per-node daily death-hazard-rate grid (\verb{n_ticks x n_nodes}, from
\code{\link[=values_map]{values_map()}}); column \code{tick} is read.}

\item{s_count, i_count, r_count}{\verb{n_ticks x n_nodes} i32 census Columns kept in
sync (mutated at column \code{tick + 1}).}

\item{births, deaths}{\verb{(n_ticks-1) x n_nodes} i32 flow Columns; column \code{tick}
receives the per-node event counts (equal; mutated).}

\item{tick}{0-based tick index.}
}
\value{
\code{NULL} (invisibly); the Columns are modified in place.
}
\description{
\code{rate} is a per-node daily death-HAZARD-rate grid (a values map; the caller
converts a crude death rate of annual deaths per 1000 people to a daily rate,
e.g. \code{cdr / 1000 / 365}). For each of the first \code{count} agents, this converts the
node's rate to a probability \code{1 - exp(-rate)} (once per node, like transmission)
and, with that probability, "unalives" the agent and replaces it with a newborn:
the agent's \code{state} is reset to Susceptible and its \code{timer} to 0.
}
\details{
The S/I/R node census is updated IN PLACE at column \code{tick + 1} (the working
column the caller has already carried forward): a death out of I decrements \code{I}
and increments \code{S}; a death out of R decrements \code{R} and increments \code{S}; a death
out of S nets to zero. Every event (from any compartment) is counted per node and
written to BOTH the \code{births} and \code{deaths} flow reports for \code{tick} (equal under
constant population). Agents are assumed to be in S, I, or R (it is the SIR
variant). Parallelized with private per-thread node buffers summed at the end.
}
