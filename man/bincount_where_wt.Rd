% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bincount.R
\name{bincount_where_wt}
\alias{bincount_where_wt}
\title{Sum a weight, per group, over the agents whose property satisfies a comparison.}
\usage{
bincount_where_wt(
  group,
  n_groups,
  prop,
  op,
  value,
  weights,
  count,
  counts = NULL,
  slot = 0L
)
}
\arguments{
\item{group}{An integer-typed \link{Column} of group indices (\code{i8}..\code{u32}) — e.g. \code{nodeid}.}

\item{n_groups}{Number of groups; a non-negative integer.}

\item{prop}{A numeric \link{Column} holding the per-agent property to test.}

\item{op}{Comparison string: one of \code{"eq"}, \code{"ne"}, \code{"lt"}, \code{"le"}, \code{"gt"}, \code{"ge"}.}

\item{value}{The threshold the property is compared against.}

\item{weights}{A numeric \link{Column} of per-agent weights to sum (any numeric type).}

\item{count}{How many leading agents to scan — the ACTIVE agent count, typically
\code{people$count}. Required (see \code{\link[=bincount_where]{bincount_where()}}): scanning the Column's full length
would tally reserved, inactive slots in an over-allocated population.}

\item{counts}{Optional numeric \link{Column} to receive the sums; when omitted a numeric
result vector is allocated and returned instead.}

\item{slot}{Which slice of \code{counts} to write when \code{counts} is supplied. Defaults to \code{0}.}
}
\value{
When \code{counts} is \code{NULL}, a numeric vector of per-group sums; otherwise \code{NULL}
invisibly (the result is written into \code{counts}).
}
\description{
The weighted twin of \code{\link[=bincount_where]{bincount_where()}}: for each group \code{g} in \verb{0..n_groups}, sums
\code{weights[i]} over the first \code{count} agents that both have \code{group[i] == g} AND satisfy
\verb{prop[i] <op> value}. Use it for predicate-filtered weighted aggregates by node — e.g.
total infectiousness of symptomatic agents per node, or person-days under five — in one
parallel pass with no copy of \code{prop} or \code{weights} into R.
}
\details{
Two output modes, as in \code{\link[=bincount_where]{bincount_where()}}: leave \code{counts} \code{NULL} (the default) and a
numeric vector of length \code{n_groups} is allocated and returned; or pass a numeric
\link{Column} \code{counts} and the per-group sums are written into its slice \code{slot}.
}
\examples{
states  <- laser_states()
state   <- allocate_scalar("u8",  5L); state$set(c(2, 2, 0, 2, 1))     # I I S I E
nodeid  <- allocate_scalar("u16", 5L); nodeid$set(c(0, 0, 1, 1, 1))
shed    <- allocate_scalar("f64", 5L); shed$set(c(1.0, 0.5, 9, 2.0, 9))# infectiousness
# total shedding of infectious (state==I) agents per node:
bincount_where_wt(nodeid, 2L, state, "eq", states[["I"]], shed, count = 5L)  # 1.5  2.0
}
\seealso{
\code{\link[=bincount]{bincount()}}, \code{\link[=bincount_wt]{bincount_wt()}}, \code{\link[=bincount_where]{bincount_where()}}.
}
