# Revert to a non-reproducible, entropy-seeded RNG (undo [`set_seed()`](https://clorton.github.io/razer/reference/set_seed.md)).

Revert to a non-reproducible, entropy-seeded RNG (undo
[`set_seed()`](https://clorton.github.io/razer/reference/set_seed.md)).

## Usage

``` r
unset_seed()
```

## Value

`NULL`, invisibly.
